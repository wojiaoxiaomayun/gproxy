# 项目优先架构重构说明

## 重构目标

将系统架构从"API Keys 独立管理"调整为"项目优先"的设计模式，使得项目成为第一位，API Keys 归属于项目之下。

## 架构变更

### 之前的架构
```
导航菜单:
- 仪表盘
- API Keys (独立页面)
- 项目管理
- 日志查看
```

### 现在的架构
```
导航菜单:
- 仪表盘
- 项目管理 (主入口)
  └─ 项目详情
     ├─ API Keys 管理
     ├─ 分组管理
     ├─ 上游配置
     └─ 项目设置
- 日志查看
```

## 数据模型关系

```
Project (项目)
├─ Upstream (上游服务配置)
├─ Group (分组)
│  ├─ RateLimitConfig (限流配置)
│  └─ ApiKey (API Keys)
└─ LogConfig (日志配置)
```

## 核心变更

### 1. 前端路由结构

**新增路由:**
- `/projects/[id]` - 项目详情页面

**移除路由:**
- `/api-keys` - 独立的 API Keys 页面（功能已整合到项目详情中）

### 2. 页面功能

#### 项目列表页 (`/projects`)
- 显示所有项目卡片
- 点击"查看详情"进入项目详情页

#### 项目详情页 (`/projects/[id]`)
包含四个标签页：

1. **API Keys 管理**
   - 查看该项目下的所有 API Keys
   - 创建新的 API Key（自动关联到当前项目）
   - 删除 API Key
   - 复制 API Key

2. **分组管理**
   - 查看项目下的分组
   - 创建新分组
   - 配置分组限流策略

3. **上游配置**
   - 查看上游服务配置
   - 添加/编辑/删除上游服务

4. **项目设置**
   - 编辑项目基本信息
   - 删除项目（危险操作）

### 3. 数据库初始化

修改了 `internal/model/db.go`，添加了 `seedTestData()` 函数：
- 在数据库初始化时自动插入测试数据
- 包括 2 个项目、3 个分组、2 个上游配置、3 个 API Keys
- 只在数据库为空时执行，避免重复插入

### 4. 导航菜单

移除了独立的"API Keys"菜单项，因为 API Keys 现在在项目详情中管理。

## 使用流程

### 创建新项目的完整流程

1. 进入"项目管理"页面
2. 点击"创建项目"按钮
3. 填写项目名称和描述
4. 进入项目详情页
5. 在"分组管理"标签创建分组
6. 在"API Keys"标签创建 API Key（选择所属分组）
7. 在"上游配置"标签配置上游服务
8. 在"项目设置"标签配置日志策略

### 管理现有项目

1. 在项目列表中找到目标项目
2. 点击"查看详情"
3. 在各个标签页中管理相应的配置

## 优势

1. **更符合实际使用场景**: 先有项目，再有 API Keys
2. **更清晰的层级关系**: 项目 → 分组 → API Keys
3. **更好的数据隔离**: 每个项目的配置独立管理
4. **更直观的操作流程**: 在项目上下文中管理所有相关配置

## 技术实现

### 前端技术栈
- Next.js 16.2.4 (App Router)
- React 19
- TypeScript
- Tailwind CSS
- COSS UI Components

### 后端技术栈
- Go 1.x
- Gin Web Framework
- GORM (SQLite)
- 纯 Go SQLite 驱动 (github.com/glebarez/sqlite)

## 测试数据

系统启动时会自动创建以下测试数据：

**项目:**
1. ES Proxy - Elasticsearch 代理项目
2. API Gateway - 通用API网关

**分组:**
- default (项目1)
- premium (项目1)
- basic (项目2)

**API Keys:**
- test-key-001 (项目1, default组)
- test-key-002 (项目1, premium组)
- test-key-003 (项目2, basic组)

## 后续优化建议

1. 实现项目创建功能的完整表单
2. 实现分组创建和限流配置功能
3. 实现上游服务的添加/编辑功能
4. 添加项目删除的确认流程
5. 添加 API Key 的启用/禁用功能
6. 实现项目级别的统计数据（真实数据）
7. 添加搜索和过滤功能
